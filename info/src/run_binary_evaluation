#!/bin/sh

set -eu

idxs='21 60 18 16 3 70 0 22 4'

for idx in $idxs; do
  echo python evaluate_binary.py -c cnn_binary_categories_${idx}.h5
  python evaluate_binary.py -c cnn_binary_categories_${idx}.h5
done
